const usersDisabled = require("../../models/UsersDisableds");
const user = require("../../models/User");
const disableds = require("../../models/Disabled");

const usersDisabledSearchService = {
    getOneById: async () => {
        try {

        } catch (error) {

        }
    }
}


module.exports = usersDisabledSearchService;
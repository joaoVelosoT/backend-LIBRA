const { Router } = require("express");
const router = Router();

// Create userDisabled
router.post("/");

// GetAll userDisabled
router.get("/");

// GetOne userDisabled
router.get("/");

// Update userDisabled
router.put("/");

// Delete userDisabled
router.delete("/");

module.exports = router;
